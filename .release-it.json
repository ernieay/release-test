{
  "git": {
    "tag": "true",
    "push": "true",
    "commit": "true"
  },
  "github": {
    "release": "true",
    "releaseName": "Release v${version}",
    "tokenRef": "GITHUB_TOKEN"
  },
  "npm": {
    "publish": "false"
  },
  "hooks": {
    "after:release": "echo Release v${version} ready\n\n tag v${version}\nhttps://${repo.host}/siteminder-au/${name}/releases/tag/${version}\nbuildkit tag build v${version}\nhttps://buildkite.com/siteminder/${name}/builds?branch=v${version}"
  }
}
